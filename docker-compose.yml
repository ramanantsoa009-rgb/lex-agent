version: "3.8"

services:
  api:
    build: .
    container_name: rag-api
    restart: unless-stopped
    ports:
      - "3002:3002"
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/docs"]
      interval: 30s
      timeout: 10s
      retries: 3